on:
  push

jobs:
  build-it-image:
    env:
      PULL_REQUEST_USER: ${{ github.event.pull_request.user.login }}
    name: build-it-image
    runs-on: self-hosted
    timeout-minutes: 20
    steps:
      - name: Test Echo
        run: echo "current user is " ${env.PULL_REQUEST_USER}


